import { Component, signal } from '@angular/core';
import { Toy } from '../../models/toy.model';

@Component({
  selector: 'app-order',
  imports: [],
  templateUrl: './order.html',
  styleUrl: './order.css',
})
export class Order {
  
}
